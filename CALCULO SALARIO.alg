algoritmo "CÁLCULO ACRÉSCIMO SALÁRIO"
//ENTRADA DADOS
    //LEIA SALÁRIO
//PROCESSAMENTO
    //CALCULAR SALÁRIO + 30%
//SAÍDA DADOS
   //CALCULO SALÁRIO

var

 salario, calculosalario: real

inicio
Escreval("informe seu salario: ")
leia(salario)
calculosalario <- salario * 1.3
Escreval("seu salario agora é R$: ", calculosalario)


fimalgoritmo
